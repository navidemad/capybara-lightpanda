[tools]
"asdf:NeoHsu/asdf-hugo" = "extended-0.162.0"
"github:sass/dart-sass" = "1.100.0"
# Dev-only JS test harness (test/js/) runs on Bun. Pinned to match
# test/js/.bun-version so `mise install` provisions the same post-Rust-rewrite
# build the predicate tests are validated against. Not a gem runtime dep.
bun = "1.3.14"
