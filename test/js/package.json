{
  "name": "capybara-lightpanda-js-tests",
  "private": true,
  "description": "Dev-only Bun harness for the injected _lightpanda predicate logic (test/js/). Not part of the gem package (gemspec ships lib/** only).",
  "scripts": {
    "test": "bun test"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "happy-dom": "^20.10.3"
  }
}
